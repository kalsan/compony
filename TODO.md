- When cancelling in an owned form, prefer Show instead of owner.Index
- Come up with something like `data_class(BaseClass){ attribute(:foo) }` -> ActiveType
- Docs: patterns catalog (doc/guide/patterns.md) + thickened pre-built docs done.
  Remaining optional: inline YARD on every DSL method (rubydoc.info), and a
  doc/cookbook/ of focused recipes (largely subsumed by patterns.md — reassess scope).