- When cancelling in an owned form, prefer Show instead of owner.Index
- Come up with something like `data_class(BaseClass){ attribute(:foo) }` -> ActiveType
- Docs: anonymized real-world patterns catalog written (doc/guide/patterns.md). Remaining:
  thicken the thin pre-built component docs (list/show/index/with_form) and add a
  doc/cookbook/ of focused recipes, both drawing on patterns.md.